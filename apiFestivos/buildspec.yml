version: 0.2

env:
  variables:
    ECR_REPOSITORY: "058264525154.dkr.ecr.us-east-2.amazonaws.com/apicontainer"
    IMAGE_TAG: "latest"

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo "Instalando Maven y verificando versiones..."
      - mvn --version
      - echo "Verificando Docker..."
      - docker --version

  pre_build:
    commands:
      - echo "Iniciando sesión en Amazon ECR..."
      - aws ecr get-login-password --region us-east-2 | docker login --username AWS --password-stdin 058264525154.dkr.ecr.us-east-2.amazonaws.com/apicontainer

  build:
    commands:
      - echo "Compilando proyecto Maven... sin contenedores"

  post_build:
    commands:
      - echo "Subiendo imagen a ECR..."
      - docker push $ECR_REPOSITORY:$IMAGE_TAG
      - echo "Build y push finalizados correctamente."

artifacts:
  files:
    - '**/*'
